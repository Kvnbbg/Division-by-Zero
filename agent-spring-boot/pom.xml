<?xml version="1.0" encoding="UTF-8"?>
<!--
  Le module n'avait AUCUN descripteur de build : ni pom.xml, ni build.gradle.
  Un AgentController.java existait seul, impossible à compiler, à tester ou à
  démarrer. Le service était donc annoncé sans jamais pouvoir tourner.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.5</version>
    <relativePath/>
  </parent>

  <groupId>fr.kvnbbg.tdaah</groupId>
  <artifactId>agent-spring-boot</artifactId>
  <version>0.1.0</version>
  <name>Division by Zero — agent</name>
  <description>Conversion de mesures dimensionnées, et refus explicite de la division par zéro.</description>

  <properties>
    <java.version>21</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
