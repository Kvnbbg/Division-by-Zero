# tdaah-agent — Spring Boot (amorce)

Voir docs/ROADMAP-AGENT-SPRING-BOOT.md.

Java 21. Maven : mvn spring-boot:run
GET /v1/agent/tools

Pas de secrets, pas de fichiers audio, pas de logique NFT ici.
